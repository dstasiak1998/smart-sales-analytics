# Copy file as .env and submit values
# .env file in .gitignore — never commit

APP_ENV=development
APP_PORT=8000
DEBUG=true

DB_HOST=localhost
DB_PORT=5432
DB_NAME=sales_analytics
DB_USER=postgres
DB_PASSWORD=postgres

API_KEY=dev-secret-key-change-in-production
STREAMLIT_PORT=8501
